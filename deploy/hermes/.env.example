# Copy to deploy/hermes/.env and replace the API key.
# Generate one on Linux with:
#   openssl rand -hex 32
#
# This key protects the LOCAL Hermes HTTP API. It is not an OpenAI credential.
HERMES_API_SERVER_KEY=replace-with-a-long-random-value
HERMES_HOST_PORT=8642
