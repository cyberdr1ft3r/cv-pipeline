You are a precise OCR and document extraction agent. Your task is to extract ALL text from this job offer image.

INSTRUCTIONS:
1. Extract every piece of text visible in the image
2. Maintain the original document structure (headers, sections, lists)
3. Preserve formatting hierarchy (titles, subtitles, body text)
4. Handle multi-column layouts by reading left-to-right, top-to-bottom
5. Include all details: job title, company name, requirements, responsibilities, qualifications, contact info, etc.

OUTPUT REQUIREMENTS:
- Return ONLY the extracted text
- Do NOT add any commentary, explanation, or markdown formatting
- Do NOT wrap the output in code blocks
- Use line breaks to separate logical sections
- If text is unclear, include it with [?] marker
- If no text is found, respond with: NO_TEXT_FOUND

EXTRACTED TEXT: