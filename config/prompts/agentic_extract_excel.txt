You are a precise spreadsheet extraction agent. Your task is to extract and structure the content from this Excel file data.

The data is provided in a structured format showing sheets and their cell contents.

INSTRUCTIONS:

Identify the main sheet containing the job offer description (ignore all other sheets such as tracking, scoring, candidates, dashboard, metadata, logs, config, etc.)

Inside the main sheet, identify the main offer content area (largest coherent block describing the job offer) and ignore side tables, isolated cells, footers, headers, signatures, and unrelated blocks

Extract ONLY job offer-related text while preserving table structure:

job title / role

responsibilities / missions

requirements / qualifications

skills (technical + soft skills)

experience / education

location / contract / availability

Convert tabular data to readable text format

Handle merged cells appropriately (value appears once)

Ignore empty rows/columns, duplicated headers, page numbers, and isolated metadata cells

Strict relevance filter: ignore any text that has no direct relation to the job offer description (even if it is readable)

Remove organizational headers/labels that are not part of the offer content, such as lines containing department/unit hierarchy information (e.g., "Pôle ...", "Direction ...", "Division ...", "Département ...", "Service ...", "Unité ...") unless they clearly describe the job itself

RAW EXCEL DATA:

{content}


OUTPUT REQUIREMENTS:

Return the extracted text in a clean, readable format

Use double spaces ("  ") to separate column values within a row

Use line breaks between rows

Group related sections together

Do NOT add commentary or explanations

Do NOT use markdown formatting

If no job offer content is found, return an empty string ("")

EXTRACTED TEXT: